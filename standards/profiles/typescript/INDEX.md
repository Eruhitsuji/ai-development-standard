# TypeScript Profile

Use this profile for TypeScript projects.

- Keep strict type checking enabled unless an approved exception exists.
- Prefer package-manager scripts over manually assembled commands.
- Do not change lockfiles outside the task scope.
- Document public API and schema changes.

Downstream projects should define formatter check, linter, type check, unit tests, and build.
