# Definition of Done

An issue is done when all acceptance criteria are met, required tests are added or exceptions exist, checks pass or are reported as not run, documentation is updated, security and compatibility impact are reviewed, implementation was reviewed by a separate party, the pull request is merged, and follow-up work is captured as separate issues.

Do not mark an issue `Done` before the required pull request is merged.
