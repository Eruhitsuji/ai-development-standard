schema_version: 1

required:
  - project.id
  - project.name
  - repository.owner
  - repository.name
  - ai.enabled_tools

fields:
  project.id:
    type: string
  project.name:
    type: string
  repository.owner:
    type: string
  repository.name:
    type: string
  ai.enabled_tools:
    type: list
